name: Trigger v0.91.0 release

on:
  push:
    branches:
      - chore/v0.91.0-release-trigger

jobs:
  validate-trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "release trigger workflow is valid"
