name: Trigger v0.91.0 release

on:
  push:
    branches:
      - chore/v0.91.0-release-trigger

permissions:
  contents: write
  actions: write

jobs:
  validate-trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "permissions are valid"
