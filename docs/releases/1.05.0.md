# Storage Agent v1.05.0

**Full teardown — all UI/UE/text rebuilt. Codex/Cursor native. No migration (head remains 027).**

v1.04 was warm paint on a web-app chassis. v1.05 removes the chassis.

## All elements rebuilt

- **Shell** `44px` activity bar (`32px` hit, `1.5px` orange indicator, `16px` optical) + `260px` tree Explorer (`28px` rows, `12px` `sm`, type-ahead, `⌘K` primary) + `46rem` document + `280px` inspector + `28px` vibrancy title bar + `22px` status bar — all hairline, no shadow.
- **Tokens** warm `h=32` ladder (`#141210` → `#363330`, `#fff9f0` → `#f5d0b0`, `dL*12.3`, `AA 4.97`) single orange `#ff6b35→#c73a00`, `400/-0.02em` display, `1.8` leading, iOS spring `120/180/240`.
- **Composer** single `contenteditable` `44→240px`, `↑` history, `⌘K` file ref, `⇧Enter` newline, `rounded-2xl` `px-4/py-3.5` `text-base`, orange pill `Delegate` + `Steer+Stop`.
- **Work Result** `46rem` prose `15px/1.8` + `64rem` wide tables sharing left edge + sticky `2×` outline, `7×7` star header, `prose` vs `wide` tracks.
- **Empty** magazine: `72px` hero `400/-2.16px` + `16px` sub + `14px` CTA, not `12px` settings.
- **Palette** `⌘K` primary with preview on hover, `J/K` walks list not doc, `⌘B`/`⌘.` taught via `Details` pane.
- **Text** `Delegate a goal` → `Ask anything — describe a storage problem, paste an error, or drop a file…`, `No tasks yet` → editorial hero.

## Preserved

- `Agent Task is the application`. Durable `task_executions` + `execution_events` (`after=<seq>`), `task_decisions`, `work_results`, `task_artifacts`, `task_context_versions`. `v1.03` gated extensions (local models, user skills, MCP, OTel, OS shell).

## Security

Same floor. No new tools, no migration past 027.

## Known limitations

- Apple notarization / Windows Authenticode / updater pubkey still gated (inert).
