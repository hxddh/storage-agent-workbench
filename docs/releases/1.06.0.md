# Storage Agent v1.06.0

**Full teardown — magazine empty + wide track fix. Codex/Cursor native at 10/10. No migration (head remains 027).**

v1.05 was warm paint on a web-app chassis. v1.06 removes the chassis and the copy.

## All elements rebuilt (final)

- **Empty** magazine hero `72px` `4.5rem` `400/-0.04em` `0.9` (`text-3xl`) + `16px/7` sub + `14px` CTA — `EmptyState` now `text-3xl` token, not `[4.5rem]` arbitrary. Fixes `design-tokens.test` 157 arbitrary check.
- **Shell** `44px` activity bar `32px` hit `1.5px` orange indicator — `App` now `w-11` with `Item` helper, `⌘B` taught.
- **Tokens** `h=32` warm `dL*12.3` `AA 4.97` already in `v1.05`, now with `text-3xl` 72px hero.
- **E2E** `da4a6d6` + `96d2c9f` fixes for `data-collapsed` + `64rem` wide track already in `v1.05`, now with `456/456` green.

## Preserved

- `Agent Task is the application`. Durable runtime, local models, user skills, MCP, OTel, OS shell.

## Security

Same floor. No new tools, no migration past 027.
