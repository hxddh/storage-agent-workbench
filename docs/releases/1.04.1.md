# Storage Agent v1.04.1

Patch for the v1.04 Codex/Cursor rebuild — fixes the 6 E2E failures from the new three-pane shell. No new storage tools, no migration (head remains **027**).

## Fixed

- **E2E: keep nav in DOM for narrow** — `App` now always renders `AgentTaskNavigation` in the DOM (width 0 when collapsed) so `getByTestId('agent-task-navigation')` is found at `900px` and `data-collapsed` toggles correctly. Fixes 4 layout/shell E2E (`folds below 1000px`, `folded navigation can still switch`, `collapses across reload`, `toggles from keyboard`).
- **E2E: allow wide Work Result track** — outer `Work Result` card is now `w-full max-w-[64rem]` (was `max-w-[46rem]`) so `.agent-result-wide` can be `> prose +100` while sharing the left edge. Fixes `data-rich Work Results use a wider track` and `both exchanges survive a reload`.

## Preserved

- Warm editorial tokens (h=32, orange), icon bar + collapsible 260px Explorer + 240px Details, Cursor-like Composer (44px, rounded-2xl), editorial Work Result (46rem prose, 64rem wide track).

## Security

Same floor. No new tools, no migration past **027**.
