# Storage Agent v1.17.3

**Patch — context economy and Codex chrome. No migration (head stays 030).**

## Summary

v1.17.2 layered prompt grounding and put Codex Search under New task. Using that build, two P0s remained: the Chat Completions prefix was still re-sent every step at ~6.8k tokens empty, and the title bar still painted Find/palette icons Codex does not. This patch cuts the per-step bill and matches Codex chrome. Chat Completions stays. No Responses-API compaction. No migration.

## What changed

- **Prefix.** Engine tools (`simulate_storage_cost`, remediation, baseline, Drift, price table, revisits) leave CORE for the gated `storage_engines` group. Tool descriptions are one sentence plus Args. The system prompt and skill-catalog preamble are shorter; every `SESSION_SAFETY_RULES` item stays verbatim in both `INSTRUCTIONS` and `FINALIZE_INSTRUCTIONS`.
- **In-turn output.** A first tool delivery over 6 000 characters is reduced to a structured digest before the model sees it (`read_skill` / `update_plan` exempt). Already-consumed outputs compact after one later step. The tool-output floor is 48k characters (12 % of the window); the turn-token ceiling is two window-equivalents, floored at 250k.
- **Chrome.** The title bar is the task name and its real state (⌘F / ⌘K stay; no painted Find/palette). Sidebar Search is a lighter row under New task. The Composer is a hairline slot, not an elevated card. The user bubble is `--text-prose` with quieter padding. Approval impact reads as stacked sentences. Find remains the 46rem strip under the title bar.

## Preserved

Same runtime, same contracts, same security floor as v1.17.2. One durable submit path. No migration. `RunConfig.group_id` still routes official OpenAI prompt cache by task. ContextMeter stays in the model menu.

## Contracts

- CORE does not include engine tools; `load_tools("storage_engines")` unlocks them.
- `_COMPACT_AFTER_STEPS = 1`; `_MAX_TOOL_OUTPUT_CHARS = 48_000`; `_FIRST_DELIVERY_CHARS = 6000`.
- Title bar has no `titlebar-find` / `titlebar-palette`; sidebar Search is `.native-sidebar-search`; Find is `.native-find-host` + `.native-find`.

## Verification performed

- Frontend: `npx tsc --noEmit` and `npx vitest run` — **646 passed**.
- Sidecar focused suites (v1173, v1172, v057, v055, v054, v0270, v0410, review_fixes, skills, v0280, v112, v111, v058, v056, v0290, v062, maxturns) — pass.
- Sidecar `ruff check app tests` — pass after removing a duplicate re-export.
- Not run locally: full sidecar suite, Playwright E2E, packaged Sidecar smoke, visual-review, desktop packaging.
- Desktop publish: via the `release/v1.17.3` pipeline (transactional; publishes only when all platforms succeed).
