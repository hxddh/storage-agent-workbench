{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Phase 01 shell. Core only; no plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
