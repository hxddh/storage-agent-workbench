{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set: core only. The bundled sidecar (a one-dir resource) is launched directly from Rust via std::process, not the shell plugin, so no shell capability is needed.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
