<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for a NOTARIZED build.

  Notarization requires the hardened runtime, but under it our bundled
  PyInstaller one-file Python sidecar cannot load its own embedded framework /
  dlopen its libraries, and it never starts. These entitlements re-allow exactly
  what the sidecar needs and nothing more:

  - disable-library-validation: load the sidecar's bundled (differently-signed
    or unsigned-at-build) Python framework and .so/.dylib files.
  - allow-dyld-environment-variables: PyInstaller's bootloader sets DYLD_* to
    locate its extracted libraries.
  - allow-unsigned-executable-memory / allow-jit: CPython uses executable memory
    for some code paths.

  Used only on the Developer-ID + notarized path (see docs/signing.md). The
  default free build is ad-hoc signed WITHOUT the hardened runtime and does not
  use this file.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
</dict>
</plist>
